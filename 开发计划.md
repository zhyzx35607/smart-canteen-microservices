# 智能食堂点餐与取餐微服务系统 — 开发计划

## 1. 项目目标

按照评分标准,完成:
- **代码部分 (50 分)**:5 个微服务 + 代码质量 + 测试覆盖
- **文档部分 (50 分)**:架构设计 + K3S 部署说明 + 软件工程文档(需求/概要/详细/测试用例/测试报告)

## 2. 总体进度安排

| 阶段 | 周数 | 主要产物 |
| --- | --- | --- |
| 阶段一:需求与设计 | 第 1 周 | 需求规格说明书、架构设计文档、概要设计 |
| 阶段二:基础搭建 | 第 2 周 | 父工程、Nacos/MySQL/Redis 环境、网关骨架 |
| 阶段三:核心服务开发 | 第 3-5 周 | 用户、菜品、订单、取餐 4 个服务 |
| 阶段四:集成联调 | 第 6 周 | 端到端流程打通、WebSocket 大屏 |
| 阶段五:测试与部署 | 第 7 周 | 单元/集成测试、K3S 部署、测试报告 |
| 阶段六:文档完善 | 第 8 周 | 详细设计、测试报告定稿 |

## 3. 详细任务清单

### 阶段一:需求与设计 (第 1 周)

- [x] 阅读题目,明确 5 个必选服务的功能边界
- [x] 编写《需求规格说明书》:功能需求、非功能需求、接口需求
- [x] 编写《架构设计文档》:架构图、技术选型、服务划分
- [x] 编写《概要设计说明书》:模块划分、数据库设计、主要接口
- [x] 确认技术栈版本对齐 (Spring Boot 3.x + Spring Cloud 2023.x + SCA 2023.x)

### 阶段二:基础环境搭建 (第 2 周)

- [x] 创建 Maven 父工程,定义统一依赖版本
- [x] 搭建 `canteen-common` 模块:统一响应体、异常、JWT 工具、常量
- [x] 使用 docker-compose 启动本地 Nacos、MySQL、Redis (开发用)
- [x] 创建 5 个服务骨架:gateway / user / menu / order / pickup
- [x] 每个服务接入 Nacos 注册 + 配置中心
- [x] 网关配置基础路由,验证 `ping` 接口可通

### 阶段三:核心服务开发 (第 3-5 周)

#### 3.1 用户服务 (3 天)
- [x] 数据库表:`user`、`refresh_token`
- [x] 注册接口:手机号/学工号唯一性校验、BCrypt 加密
- [x] 登录接口:密码登录、验证码登录(固定 123456)
- [x] JWT 颁发:accessToken + refreshToken
- [x] `/users/me` 查询与修改
- [x] `/auth/refresh` 和 `/auth/logout`(Redis 黑名单)

#### 3.2 菜品菜单服务 (3 天)
- [x] 数据库表:`merchant`、`dish`、`daily_menu`、`daily_menu_item`
- [x] 菜品 CRUD + 上下架
- [x] 每日菜单发布(售卖时段、库存初始化到 Redis)
- [x] 库存 Lua 脚本:原子扣减、回滚
- [x] 低库存 WARN 日志
- [x] 内部接口 `/internal/stock/deduct`、`/internal/stock/restore`

#### 3.3 订单服务 (4 天)
- [x] 数据库表:`orders`、`order_item`
- [x] 下单:幂等 → Feign 扣库存 → 落库 → 发延时消息
- [x] 订单状态机:PLACED → ACCEPTED → PREPARING → WAITING_PICKUP → PICKED_UP
- [x] 取消订单:用户主动 / 超时自动,回滚库存
- [x] 取餐码生成(6 位数字,不重复)
- [x] 订单详情、订单列表(分页)
- [x] Feign 客户端调菜品/取餐服务

#### 3.4 取餐排队服务 (3 天)
- [x] Redis 队列数据结构设计
- [x] 入队接口(内部,订单服务调用)
- [x] 叫号接口(商家端)
- [x] 核销接口(用户/窗口端)
- [x] 大屏查询接口
- [x] WebSocket 端点 `/ws/screen/{counterId}`

#### 3.5 网关服务 (2 天)
- [x] 从 Nacos 动态加载路由
- [x] JWT 校验全局过滤器(带白名单)
- [x] TraceId 透传
- [x] Sentinel 限流(IP 级 + 用户级)
- [x] WebSocket `lb:ws://` 代理
- [x] 统一异常响应格式

### 阶段四:集成联调 (第 6 周)

- [x] 端到端流程:注册 → 登录 → 浏览菜单 → 下单 → 接单 → 制作 → 叫号 → 核销
- [x] WebSocket 大屏前端页面(简单 HTML + JS,能连 WS 显示叫号)
- [x] 边界测试:库存不足、重复下单、超时取消、重复核销
- [x] 日志统一 TraceId,便于跨服务排查

### 阶段五:测试与部署 (第 7 周)

#### 5.1 测试
- [x] 每个服务关键业务逻辑单元测试(JUnit 5 + Mockito)
- [x] 订单主链路集成测试(Testcontainers 起 MySQL + Redis)
- [x] 编写《测试用例》:覆盖注册、登录、下单、取餐、异常场景
- [x] 执行测试并编写《测试报告》:结果统计、缺陷记录、通过判定

#### 5.2 K3S 部署
- [x] 每个服务编写 Dockerfile,推送本地镜像仓库
- [x] 编写 Kubernetes 清单(Deployment / Service / ConfigMap / Secret)
- [x] 基础设施清单(MySQL StatefulSet + PVC、Redis、Nacos)
- [x] pickup-service 配置 `sessionAffinity: ClientIP` 保证 WS 黏连
- [x] gateway NodePort 30080 对外暴露
- [x] `kubectl apply -k` 一键部署验证
- [x] 端到端 smoke test 在集群上跑通

### 阶段六:文档完善 (第 8 周)

- [x] 编写《详细设计说明书》:关键类、算法、数据结构、接口参数
- [x] 补充《K3S 部署方案说明》:清单、标签选择器、端口映射
- [x] 整理所有文档、格式统一

## 4. 风险与应对

| 风险 | 应对 |
| --- | --- |
| Spring Boot 3 与 SCA 版本不兼容 | 先锁定官方兼容矩阵,父 POM 统一管理 |
| K3S 部署环境依赖(Nacos/MySQL)复杂 | 先本地 docker-compose 跑通再上 K3S |
| WebSocket 在多副本下推送漂移 | sessionAffinity + MQ 广播模式 |
| 库存并发扣减脏数据 | Redis Lua 原子脚本 |
| 时间不够 | 优先保 5 个服务主链路跑通,再补测试和文档 |

## 6. 交付物清单

```
smart-canteen/
├── README.md
├── 开发计划.md
├── docs/                              # 设计文档
│   ├── 架构设计文档.md
│   ├── K3S部署方案说明.md
│   ├── 需求规格说明书.md
│   ├── 概要设计说明书.md
│   ├── 详细设计说明书.md
│   ├── 测试用例.md
│   └── 测试报告.md
└── canteen-backend/
    ├── pom.xml
    ├── canteen-common/
    ├── canteen-gateway/
    ├── canteen-user/
    ├── canteen-menu/
    ├── canteen-order/
    ├── canteen-pickup/
    ├── deploy/                        # K3S 清单
    │   ├── infra/
    │   └── apps/
    └── scripts/                       # 工具脚本
        ├── docker-compose.yaml
        ├── init-db.sql
        ├── common.yaml
        └── e2e.sh
```
