# 详细设计说明书

| 项目 | 内容 |
| --- | --- |
| 项目名称 | 智能食堂点餐与取餐微服务系统 |
| 文档版本 | v1.0 |
| 编写日期 | 2026-05-21 |

---

## 1 引言

本文档描述各微服务模块内部的详细实现，包括关键类、算法逻辑、数据结构、接口参数等，直接指导编码。

---

## 2 canteen-common 公共模块

### 2.1 Result 统一响应体

```java
public class Result<T> {
    private int code;      // 0=成功，其余为错误码
    private String message;
    private T data;
    private String traceId;
}
```

### 2.2 ResultCode 错误码枚举

| 枚举值 | code | message |
| --- | --- | --- |
| SUCCESS | 0 | 操作成功 |
| UNKNOWN_ERROR | 90000 | 系统内部错误 |
| USER_ALREADY_EXISTS | 10001 | 账号已存在 |
| USER_PASSWORD_ERROR | 10002 | 密码错误 |
| USER_TOKEN_INVALID | 10003 | Token 无效或已过期 |
| USER_TOKEN_EXPIRED | 10004 | Token 已过期 |
| USER_NOT_FOUND | 10005 | 用户不存在 |
| USER_PHONE_EXISTS | 10006 | 手机号已注册 |
| USER_VERIFY_CODE_ERROR | 10007 | 验证码错误 |
| USER_DISABLED | 10008 | 账号已被禁用 |
| MENU_STOCK_INSUFFICIENT | 20001 | 库存不足 |
| MENU_DISH_OFF_SHELF | 20002 | 菜品已下架 |
| MENU_DISH_NOT_FOUND | 20003 | 菜品不存在 |
| MENU_ALREADY_PUBLISHED | 20004 | 今日菜单已发布 |
| MENU_MERCHANT_NOT_FOUND | 20005 | 商户不存在 |
| ORDER_STATUS_ILLEGAL | 30001 | 订单状态非法 |
| ORDER_EXPIRED | 30002 | 订单已过期 |
| ORDER_NOT_FOUND | 30003 | 订单不存在 |
| ORDER_CANNOT_CANCEL | 30004 | 订单当前状态不可取消 |
| ORDER_IDEMPOTENT_DUPLICATE | 30005 | 重复请求 |
| PICKUP_CODE_NOT_FOUND | 40001 | 取餐码不存在 |
| PICKUP_ALREADY_VERIFIED | 40002 | 重复核销 |
| PICKUP_QUEUE_EMPTY | 40003 | 队列为空 |
| GATEWAY_RATE_LIMITED | 50001 | 请求过于频繁，请稍后再试 |
| GATEWAY_AUTH_FAILED | 50002 | 鉴权失败 |
| GATEWAY_SERVICE_UNAVAILABLE | 50003 | 服务暂不可用 |

### 2.3 JwtTokenProvider

- 算法：HS256
- AccessToken：`sub=userId, jti=uuid, role=user, exp=15min`
- RefreshToken：`sub=userId, jti=uuid, role=refresh, exp=7d`
- 黑名单：Redis `SET jwt:bl:{jti} 1 EX {剩余秒数}`

### 2.4 InternalTokenInterceptor

Feign 请求拦截器，自动注入 `X-Internal-Token` header，值从配置 `internal.token` 读取。

### 2.5 TraceIdFilter

Servlet Filter，生成/透传 `X-Trace-Id` 到 MDC。

---

## 3 user-service 用户服务

### 3.1 分层结构

```mermaid
classDiagram
    class AuthController {
        +register(RegisterRequest) TokenResponse
        +login(LoginRequest) TokenResponse
        +refresh(RefreshRequest) TokenResponse
        +logout(userId, token) void
    }
    class UserController {
        +getProfile(userId) UserVO
        +updateProfile(userId, UpdateRequest) void
    }
    class AuthService {
        +register(RegisterRequest) TokenResponse
        +login(LoginRequest) TokenResponse
        +refresh(RefreshRequest) TokenResponse
        +logout(userId, token) void
        -generateTokenPair(User) TokenResponse
    }
    class UserProfileService {
        +getProfile(userId) UserVO
        +updateProfile(userId, UpdateRequest) void
    }
    class UserMapper {
        <<interface>>
        +selectById(id) User
        +selectOne(wrapper) User
        +selectCount(wrapper) long
        +insert(user) int
    }
    class RefreshTokenMapper {
        <<interface>>
        +insert(token) int
        +selectCount(wrapper) long
        +delete(wrapper) int
    }
    class User {
        +Long id
        +String phone
        +String studentNo
        +String passwordHash
        +String nickname
        +Integer status
    }
    class TokenResponse {
        +String accessToken
        +String refreshToken
        +Long expiresIn
        +Long userId
    }
    class JwtTokenProvider {
        +generateAccessToken(userId, role) String
        +generateRefreshToken(userId) String
        +validate(token) boolean
        +parseToken(token) Claims
    }
    class StringRedisTemplate {
        +set(key, value, ttl) void
        +hasKey(key) boolean
    }

    AuthController --> AuthService
    UserController --> UserProfileService
    AuthService --> UserMapper
    AuthService --> RefreshTokenMapper
    AuthService --> JwtTokenProvider
    AuthService --> StringRedisTemplate : 黑名单
    UserProfileService --> UserMapper
    AuthService ..> TokenResponse : 返回
    AuthService ..> User : 操作
    UserMapper ..> User
```

### 3.2 AuthService 算法逻辑

**注册**：
1. 校验手机号唯一性（`SELECT COUNT`）
2. 校验学工号唯一性
3. BCrypt（cost=10）加密密码
4. 插入 user 表
5. 调用 `generateTokenPair()` 返回双 Token

**登录**：
1. 按手机号查询用户
2. 判断用户状态（0=禁用）
3. 根据 `loginType` 校验密码或验证码
4. 调用 `generateTokenPair()` 返回双 Token

**刷新**：
1. 校验 refreshToken 有效性
2. 解析 claims，检查 role=refresh
3. 查 refresh_token 表确认 jti 未吊销
4. 删除旧 refresh_token 记录，颁发新双 Token

**登出**：
1. 解析 accessToken，获取 jti
2. Redis `SET jwt:bl:{jti} 1 EX {剩余TTL}`
3. 删除该用户所有 refresh_token 记录

### 3.3 认证流程序列图

```mermaid
sequenceDiagram
    participant C as 客户端
    participant AC as AuthController
    participant AS as AuthService
    participant DB as MySQL
    participant JWT as JwtTokenProvider
    participant R as Redis

    Note over C,R: === 注册流程 ===
    C->>AC: POST /auth/register
    AC->>AS: register(request)
    AS->>DB: SELECT COUNT phone
    DB-->>AS: 0 (未注册)
    AS->>DB: SELECT COUNT studentNo
    DB-->>AS: 0
    AS->>AS: BCrypt(password, cost=10)
    AS->>DB: INSERT user
    AS->>JWT: generateAccessToken + refreshToken
    JWT-->>AS: 双 Token
    AS->>DB: INSERT refresh_token
    AS-->>C: TokenResponse

    Note over C,R: === 登录流程 ===
    C->>AC: POST /auth/login
    AC->>AS: login(request)
    AS->>DB: SELECT by phone
    DB-->>AS: User
    AS->>AS: check status != 0
    AS->>AS: BCrypt.checkpw(password, hash)
    AS->>JWT: generateTokenPair
    JWT-->>AS: 双 Token
    AS->>DB: INSERT refresh_token
    AS-->>C: TokenResponse

    Note over C,R: === 登出流程 ===
    C->>AC: POST /auth/logout
    AC->>AS: logout(userId, token)
    AS->>JWT: parseToken(token)
    JWT-->>AS: jti, expiresAt
    AS->>R: SET jwt:bl:{jti} 1 EX {ttl}
    AS->>DB: DELETE refresh_token WHERE user_id
    AS-->>C: void
```

### 3.4 接口参数

**POST /auth/register**

```json
{
  "phone": "13800138000",       // 必填，11位手机号
  "studentNo": "2024001",       // 选填，学工号
  "password": "password123",    // 必填，6-20位
  "nickname": "昵称"            // 选填
}
```

**POST /auth/login**

```json
{
  "phone": "13800138000",       // 必填
  "loginType": "password",      // password | sms
  "password": "xxx",            // loginType=password时必填
  "verifyCode": "123456"        // loginType=sms时必填
}
```

**响应 TokenResponse**

```json
{
  "accessToken": "eyJhbGci...",
  "refreshToken": "eyJhbGci...",
  "expiresIn": 900,
  "userId": 1
}
```

---

## 4 menu-service 菜品菜单服务

### 4.1 分层结构

```mermaid
classDiagram
    class DishController {
        +listDishes(page, size, merchantId, onShelf) Page~DishVO~
        +getDish(id) DishVO
        +createDish(CreateDishRequest) void
        +updateDish(id, UpdateDishRequest) void
    }
    class DailyMenuController {
        +getTodayMenu(page, size) Page~DailyMenuVO~
        +publishMenu(PublishRequest) void
    }
    class InternalStockController {
        +deduct(StockDeductRequest) boolean
        +restore(StockRestoreRequest) void
    }
    class DishService {
        +listDishes(page, size, merchantId, onShelf) Page~DishVO~
        +getDish(id) Dish
        +createDish(request) void
        +updateDish(id, request) void
    }
    class DailyMenuService {
        +getTodayMenu(page, size) Page~DailyMenuVO~
        +publishMenu(request) void
    }
    class StockService {
        +deduct(items) boolean
        +restore(items) void
    }
    class DishMapper {
        <<interface>>
        +selectPage(page, wrapper) Page~Dish~
        +selectById(id) Dish
        +insert(dish) int
    }
    class DailyMenuMapper {
        <<interface>>
        +selectPage(page, wrapper) Page~DailyMenu~
        +insert(menu) int
    }
    class DailyMenuItemMapper {
        <<interface>>
        +insert(item) int
        +deductStockLeft(dishId, quantity) int
        +addStockLeft(dishId, quantity) int
    }
    class MerchantMapper {
        <<interface>>
        +selectById(id) Merchant
    }
    class Dish {
        +Long id
        +String name
        +Integer priceCents
        +Integer onShelf
        +Long merchantId
        +Integer threshold
    }
    class StringRedisTemplate {
        +execute(luaScript, keys, args) Object
        +get(key) String
    }

    DishController --> DishService
    DailyMenuController --> DailyMenuService
    InternalStockController --> StockService
    DishService --> DishMapper
    DishService --> MerchantMapper
    DailyMenuService --> DailyMenuMapper
    DailyMenuService --> DailyMenuItemMapper
    DailyMenuService --> DishMapper
    StockService --> StringRedisTemplate : Lua脚本
    StockService --> DailyMenuItemMapper
    StockService --> DishMapper
    DishMapper ..> Dish
```

### 4.2 StockService 核心算法

**库存扣减（Redis Lua 原子脚本）**：

```lua
-- KEYS[i] = stock:{dishId}, ARGV[i] = quantity
for i = 1, #KEYS do
  local left = tonumber(redis.call('GET', KEYS[i]))
  if not left or left < tonumber(ARGV[i]) then return 0 end
end
for i = 1, #KEYS do
  redis.call('DECRBY', KEYS[i], ARGV[i])
end
return 1
```

- 返回 1 = 扣减成功，0 = 库存不足
- 先检查所有菜品库存是否充足，再批量扣减，保证原子性

**库存扣减 - 数据一致性策略**：

Redis 是请求时事实数据源，DB 作为持久化备份；二者通过 deduct/restore 双写保持一致。

1. Redis Lua 原子检查并扣减 `stock:{dishId}`
2. 成功后同步落库：`UPDATE daily_menu_item SET stock_left = stock_left - quantity WHERE dish_id = ? AND stock_left >= quantity`
3. 若 DB 更新行数为 0，记录 WARN 日志（不阻塞业务）

**库存回滚**：
1. Redis `INCRBY stock:{dishId} {quantity}`
2. MySQL `UPDATE daily_menu_item SET stock_left = stock_left + quantity`

**低库存预警**：
- 扣减后检查 `left < threshold`，记录 WARN 日志

### 4.3 库存扣减/回滚序列图

```mermaid
sequenceDiagram
    participant OS as Order Service
    participant SC as StockController
    participant SS as StockService
    participant R as Redis
    participant DB as MySQL

    rect rgb(255,245,245)
        Note over OS,DB: === 扣减库存 ===
        OS->>SC: POST /internal/stock/deduct
        SC->>SS: deduct(items)
        SS->>R: EVAL DEDUCT_LUA
        R->>R: FOR each: GET stock:{id}
        R->>R: 检查 all >= qty
        alt 全部充足
            R->>R: FOR each: DECRBY stock:{id}
            R-->>SS: return 1
            SS->>DB: UPDATE daily_menu_item SET stock_left = stock_left - qty
            SS->>SS: if stock_left < threshold → WARN log
            SS-->>OS: 扣减成功
        else 任一不足
            R-->>SS: return 0
            SS-->>OS: 库存不足 (20001)
        end
    end

    rect rgb(245,245,255)
        Note over OS,DB: === 回滚库存 ===
        OS->>SC: POST /internal/stock/restore
        SC->>SS: restore(items)
        SS->>R: FOR each: INCRBY stock:{id}
        SS->>DB: UPDATE daily_menu_item SET stock_left = stock_left + qty
        SS-->>OS: 回滚成功
    end
```

### 4.4 DailyMenuService 发布菜单

1. 校验同日同商户不能重复发布
2. 插入 `daily_menu` 记录
3. 逐条插入 `daily_menu_item`，初始化 Redis 库存 `SET stock:{dishId} {stockInit}`

---

## 5 order-service 订单服务

### 5.1 分层结构

```mermaid
classDiagram
    class OrderController {
        +placeOrder(PlaceOrderRequest) OrderVO
        +getOrder(orderId) OrderVO
        +listMyOrders(page, size) Page~OrderVO~
        +cancelOrder(orderId) void
    }
    class InternalOrderController {
        +markPickedUp(orderId) void
        +markAccepted(orderId) void
        +markPreparing(orderId) void
        +markReady(orderId) void
    }
    class OrderService {
        +placeOrder(userId, request, idempotencyKey) OrderVO
        +getOrder(orderId) Order
        +listMyOrders(userId, page, size) Page~OrderVO~
        +cancelOrder(orderId) void
        +timeoutCancel(orderId) void
        -generatePickupCode(counterId) String
    }
    class OrderStateMachine {
        +transition(currentStatus, event) String
    }
    class OrderMapper {
        <<interface>>
        +insert(order) int
        +selectById(id) Order
        +selectPage(page, wrapper) Page~Order~
        +updateById(order) int
    }
    class OrderItemMapper {
        <<interface>>
        +insert(item) int
        +selectList(wrapper) List~OrderItem~
    }
    class Order {
        +Long id
        +Long userId
        +Long merchantId
        +String counterId
        +String status
        +Integer totalCents
        +LocalDateTime placedAt
        +LocalDateTime pickedAt
    }
    class OrderItem {
        +Long id
        +Long orderId
        +Long dishId
        +String dishName
        +Integer unitPrice
        +Integer quantity
    }
    class RocketMQConfig {
        +sendTimeoutMessage(orderId) void
    }
    class StringRedisTemplate {
        +setIfAbsent(key, value, ttl, unit) Boolean
        +increment(key) Long
    }
    class MenuDishClient {
        <<Feign>>
        +getDish(dishId) Result~Dish~
    }
    class MenuMerchantClient {
        <<Feign>>
        +getMerchant(merchantId) Result~Merchant~
    }
    class MenuServiceClient {
        <<Feign>>
        +deduct(request) Result~boolean~
        +restore(request) void
    }
    class PickupServiceClient {
        <<Feign>>
        +enqueue(request) void
    }
    class OrderTimeoutListener {
        +onMessage(orderId) void
    }

    OrderController --> OrderService
    InternalOrderController --> OrderService
    OrderService --> OrderMapper
    OrderService --> OrderItemMapper
    OrderService --> OrderStateMachine
    OrderService --> MenuDishClient
    OrderService --> MenuMerchantClient
    OrderService --> MenuServiceClient
    OrderService --> PickupServiceClient
    OrderService --> StringRedisTemplate
    OrderService --> RocketMQConfig
    OrderTimeoutListener --> OrderService : timeoutCancel()
    OrderMapper ..> Order
    OrderItemMapper ..> OrderItem
```

### 5.2 OrderStateMachine 状态机

```mermaid
stateDiagram-v2
    [*] --> PLACED : 用户下单
    
    PLACED --> ACCEPTED : 商家接单(accept)
    PLACED --> CANCELED : 取消订单(cancel)
    
    ACCEPTED --> PREPARING : 开始制作(start)
    ACCEPTED --> CANCELED : 取消订单(cancel)
    
    PREPARING --> WAITING_PICKUP : 制作完成(ready)
    
    WAITING_PICKUP --> PICKED_UP : 用户取餐(pickup)
    
    PICKED_UP --> [*]
    CANCELED --> [*]
    
    note right of PLACED
        已下单
        等待商家接单
    end note
    
    note right of ACCEPTED
        商家已接单
        准备开始制作
    end note
    
    note right of PREPARING
        制作中
    end note
    
    note right of WAITING_PICKUP
        待取餐
        已入队等待叫号
    end note
    
    note right of PICKED_UP
        已取餐
        订单完成
    end note
    
    note right of CANCELED
        已取消
        库存已回滚
    end note
```

- `cancel` 仅允许从 `PLACED` 和 `ACCEPTED` 状态发起
- `PREPARING` 及之后不允许取消

### 5.3 OrderService.placeOrder 算法

1. **幂等校验**：Redis `SETNX order:idem:{key} 1 EX 60`
2. **计算总价**：遍历 items，Feign 获取菜品最新价格和上下架状态
3. **扣减库存**：Feign → menu-service `POST /internal/stock/deduct`
4. **查询窗口**：Feign → menu-service `GET /internal/merchants/{id}` 反查 counter_id
5. **落库**：本地事务插入 `orders` + `order_item`，状态 PLACED
6. **延时消息**：RocketMQ 发送 `order.timeout` 延时 30min（DELAY level = 16）
7. **失败回滚**：若第 3 步失败抛异常，不回滚（未落库）；若第 5 步失败，事务回滚由 Spring 管理；超时 / 用户取消触发 `restore`
```mermaid
flowchart TD
    A["用户发起下单请求"] --> B["Step 1: 幂等校验"]
    B --> C{"idempotencyKey\nRedis SETNX?"}
    C -->|已存在| D["返回 ORDER_IDEMPOTENT_DUPLICATE(30005)"]
    C -->|首次请求| E["Step 2: 校验菜品"]
    E --> F["遍历 items\nFeign调用 menuDishClient.getDish()"]
    F --> G{"菜品存在且\nonShelf=1?"}
    G -->|否| H["返回 MENU_DISH_NOT_FOUND(20003)\n或 MENU_DISH_OFF_SHELF(20002)"]
    G -->|是| I["计算总价 totalCents\n构造 orderItems 和 stockItems"]
    I --> J["Step 3: 扣减库存"]
    J --> K["Feign调用\nmenuServiceClient.deduct(stockItems)"]
    K --> L{"扣减成功?"}
    L -->|否| M["返回 MENU_STOCK_INSUFFICIENT(20001)"]
    L -->|是| N["Step 4: 查询窗口"]
    N --> O["Feign调用\nmenuMerchantClient.getMerchant()"]
    O --> P{"商户存在?"}
    P -->|否| Q["返回 MENU_MERCHANT_NOT_FOUND(20005)"]
    P -->|是| R["获取 counterId"]
    R --> S["Step 5: 写入订单\n@Transactional"]
    S --> T["插入 Order 记录\n状态 = PLACED"]
    T --> U["批量插入 OrderItem 记录"]
    U --> V["Step 6: 发送延时消息"]
    V --> W["RocketMQ 延时消息\ndelay level=16 (约30分钟)"]
    W --> X["Step 7: 返回结果"]
    X --> Y["返回 OrderVO {\norderId, totalCents,\nstatus=PLACED, ...}"]
    
    style D fill:#f96,stroke:#333
    style H fill:#f96,stroke:#333
    style M fill:#f96,stroke:#333
    style Q fill:#f96,stroke:#333
    style Y fill:#9f6,stroke:#333
```

### 5.4 超时自动取消序列图

```mermaid
sequenceDiagram
    participant MQ as RocketMQ
    participant OL as OrderTimeoutListener
    participant OS as OrderService
    participant OSM as OrderStateMachine
    participant MS as Menu Service (Feign)
    participant DB as MySQL

    MQ->>OL: 延时消息 (orderId, 30min后)
    OL->>OS: timeoutCancel(orderId)
    OS->>DB: SELECT order by id
    DB-->>OS: Order (status)
    
    alt status == PLACED or ACCEPTED
        OS->>OSM: transition(current, "cancel")
        OSM-->>OS: CANCELED
        OS->>OS: 从 order.items 提取 dishId+quantity
        OS->>MS: Feign: POST /internal/stock/restore
        MS-->>OS: 回滚成功
        OS->>DB: UPDATE order SET status=CANCELED, cancelReason="超时自动取消"
        OS-->>OL: 取消完成
    else status == PREPARING or later
        OS-->>OL: 订单已进入制作，不取消 (log.info skip)
    end
```

### 5.5 取餐码生成

```java
private String generatePickupCode(String counterId) {
    Long seq = redis.incr("pickup:code:seq:" + counterId);
    return String.format("%s%06d", counterId, seq % 1000000);
}
```

### 5.6 OrderTimeoutListener

消费 `order.timeout` 主题，若订单仍为 `PLACED`/`ACCEPTED`，则：
1. 状态置为 `CANCELED`
2. 回滚库存（Feign → menu-service `/internal/stock/restore`）

---

## 6 pickup-service 取餐排队服务

### 6.1 分层结构

```mermaid
classDiagram
    class PickupController {
        +enqueue(PickupEnqueueRequest) void
        +verify(VerifyRequest) void
        +call(counterId) CallEvent
        +getScreenData(counterId) QueueScreenVO
        +getQueueStatus(counterId) QueueStatusVO
    }
    class PickupQueueService {
        +enqueue(request) void
        +verify(request) void
        +call(counterId) CallEvent
        +getScreenData(counterId) QueueScreenVO
        +getQueueStatus(counterId) QueueStatusVO
    }
    class StringRedisTemplate {
        +rpush(key, value) Long
        +lpop(key) String
        +lrange(key, start, end) List
        +ltrim(key, start, end) void
        +set(key, value, ttl, unit) void
        +get(key) String
        +del(key) void
    }
    class SimpMessagingTemplate {
        +convertAndSend(destination, payload) void
    }
    class OrderServiceClient {
        <<Feign>>
        +markPickedUp(orderId) void
    }
    class WebSocketConfig {
        +configureMessageBroker(registry) void
        +registerStompEndpoints(registry) void
    }
    class QueueEntry {
        +Long orderId
        +String pickupCode
        +Long userId
    }
    class CallEvent {
        +String counterId
        +QueueEntry entry
        +String eventType
    }
    class QueueScreenVO {
        +String counterId
        +QueueEntry currentCalling
        +List~QueueEntry~ waitingList
        +List~QueueEntry~ historyList
    }

    PickupController --> PickupQueueService
    PickupQueueService --> StringRedisTemplate : Redis队列操作
    PickupQueueService --> SimpMessagingTemplate : WebSocket推送
    PickupQueueService --> OrderServiceClient : Feign调用
    WebSocketConfig ..> SimpMessagingTemplate : 配置
    PickupQueueService ..> QueueEntry
    PickupQueueService ..> CallEvent
    PickupQueueService ..> QueueScreenVO
```

### 6.2 PickupQueueService 核心逻辑

**入队**：
1. `RPUSH queue:{counterId} {json: orderId, pickupCode, userId}`
2. 反向索引 `SET code:{counterId}:{pickupCode} {orderId} EX 86400`
3. 反向索引 `SET pickup:counter:{pickupCode} {counterId} EX 86400` （核销时按取餐码定位窗口）

**叫号**：
1. `LPOP queue:{counterId}` 取出队首
2. `SET queue:current:{counterId} {json} EX 86400`
3. `RPUSH queue:history:{counterId} {json}`（截断 ≤ 50）
4. WebSocket 广播 `CallEvent{counterId, entry, type="CALL"}` 到 `/topic/screen/{counterId}`

**核销**：
1. 若请求未传 counterId，通过 `GET pickup:counter:{pickupCode}` 反查
2. 通过反向索引 `code:{counterId}:{pickupCode}` 查找 orderId
3. Feign → order-service `POST /internal/orders/{id}/picked`
4. 删除两个反向索引（`code:*` 与 `pickup:counter:*`）和 current 记录
5. WebSocket 广播 `CallEvent{type="VERIFY"}`

**大屏数据**：
1. `GET queue:current:{counterId}` → 当前叫号
2. `LRANGE queue:{counterId} 0 -1` → 等待队列
3. `LRANGE queue:history:{counterId} -10 -1` → 最近 10 条历史

### 6.3 排队叫号序列图

```mermaid
sequenceDiagram
    participant M as 商家端
    participant PC as PickupController
    participant PS as PickupQueueService
    participant R as Redis
    participant OS as Order Service (Feign)
    participant WS as WebSocket

    Note over M,WS: === 入队 (enqueue) ===
    OS->>PC: POST /internal/queues/enqueue
    PC->>PS: enqueue(request)
    PS->>R: SET code:{cid}:{pickupCode} = orderId (24h)
    PS->>R: SET pickup:counter:{pickupCode} = counterId (24h)
    PS->>R: RPUSH queue:{counterId} {entry}
    PS-->>OS: 入队成功

    Note over M,WS: === 叫号 (call) ===
    M->>PC: POST /queues/{counterId}/call
    PC->>PS: call(counterId)
    PS->>R: LPOP queue:{counterId}
    alt 队列非空
        R-->>PS: QueueEntry
        PS->>R: SET queue:current:{counterId} = entry (24h)
        PS->>R: RPUSH queue:history:{counterId} entry
        PS->>WS: STOMP /topic/screen/{counterId} (CALL)
        PS-->>M: CallEvent
    else 队列为空
        R-->>PS: nil
        PS-->>M: PICKUP_QUEUE_EMPTY (40003)
    end

    Note over M,WS: === 核销 (verify) ===
    M->>PC: POST /pickups/verify
    PC->>PS: verify(request)
    alt 有 counterId
        PS->>R: GET code:{counterId}:{pickupCode}
    else 无 counterId
        PS->>R: GET pickup:counter:{pickupCode}
        R-->>PS: counterId
        PS->>R: GET code:{counterId}:{pickupCode}
    end
    R-->>PS: orderId
    PS->>OS: Feign: POST /internal/orders/{orderId}/picked
    OS-->>PS: 标记成功
    PS->>R: DEL code:{cid}:{pickupCode}
    PS->>WS: STOMP /topic/screen/{counterId} (VERIFY)
    PS-->>M: 核销成功
```

### 6.4 WebSocket 配置

- STOMP 协议，端点 `/ws/screen`
- 客户端订阅 `/topic/screen/{counterId}`
- 服务端通过 `SimpMessagingTemplate.convertAndSend()` 推送

---

## 7 gateway-service 网关服务

### 7.1 全局过滤器链

| 顺序 | 过滤器 | 职责 |
| --- | --- | --- |
| -100 | TraceIdGlobalFilter | 生成/透传 X-Trace-Id |
| -60 | RateLimitFilter | IP 级 + 用户级限流（Redis 计数器） |
| -50 | JwtAuthGlobalFilter | JWT 校验 + 黑名单检查 + 下游 header 注入 |
| -1 | GlobalErrorFilter | 统一异常响应 |
```mermaid
flowchart TD
    A["HTTP请求进入Gateway"] --> B["TraceIdGlobalFilter<br/>(order=-100)"]
    B --> B1["读取/生成 X-Trace-Id"]
    B1 --> B2["注入 MDC"]
    B2 --> C["RateLimitFilter<br/>(order=-60)"]
    C --> C1{"限流检查"}
    C1 -->|"登录/注册 IP<br/>10次/分钟"| C2{"超限?"}
    C1 -->|"下单 user<br/>20次/分钟"| C2
    C1 -->|"通用 IP<br/>100次/分钟"| C2
    C1 -->|"已登录 user<br/>60次/分钟"| C2
    C2 -->|是| C3["返回 429<br/>Too Many Requests"]
    C2 -->|否| D["JwtAuthGlobalFilter<br/>(order=-50)"]
    D --> D1{"白名单匹配?<br/>/api/user/auth/**<br/>/ws/screen/**<br/>/actuator/**"}
    D1 -->|是| D2["直接放行"]
    D1 -->|否| D3["提取 Authorization<br/>Bearer token"]
    D3 --> D4{"JWT签名<br/>与有效期校验?"}
    D4 -->|失败| D5["返回 401<br/>Unauthorized"]
    D4 -->|通过| D6{"Redis黑名单<br/>jwt:bl:{jti}?"}
    D6 -->|命中| D5
    D6 -->|未命中| D7["解析 userId, role<br/>注入Header:<br/>X-User-Id<br/>X-User-Role"]
    D2 --> E["路由转发<br/>(Nacos服务发现)"]
    D7 --> E
    E --> E1["匹配路由规则<br/>lb://{service}"]
    E1 --> E2["StripPrefix<br/>去除 /api/{service} 前缀"]
    E2 --> F["下游微服务"]
    
    C3 --> G["GlobalErrorFilter<br/>(order=-1)"]
    D5 --> G
    G --> G1["统一异常响应:<br/>{ code, message, traceId }"]
    
    style C3 fill:#f96,stroke:#333
    style D5 fill:#f96,stroke:#333
    style G1 fill:#f96,stroke:#333
    style F fill:#9f6,stroke:#333
```

### 7.2 路由配置

| 路由 ID | URI | 路径匹配 | StripPrefix |
| --- | --- | --- | --- |
| user | lb://user-service | /api/user/** | 2 |
| menu | lb://menu-service | /api/menu/** | 2 |
| order | lb://order-service | /api/order/** | 2 |
| pickup | lb://pickup-service | /api/pickup/** | 2 |
| pickup-ws | lb:ws://pickup-service | /ws/screen/** | 0 |

### 7.3 限流规则

| 维度 | 限流对象 | 阈值 | 窗口 |
| --- | --- | --- | --- |
| 服务级（Sentinel） | user-service | 100 QPS | 1s |
| 服务级（Sentinel） | order-service | 50 QPS | 1s |
| IP 级（Redis） | 登录/注册接口 | 10 次/IP | 60s |
| IP 级（Redis） | 所有接口 | 100 次/IP | 60s |
| 用户级（Redis） | 下单接口 | 20 次/用户 | 60s |
| 用户级（Redis） | 所有接口 | 60 次/用户 | 60s |
