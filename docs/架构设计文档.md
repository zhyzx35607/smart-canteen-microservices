# 架构设计文档

| 项目 | 内容 |
| --- | --- |
| 项目名称 | 智能食堂点餐与取餐微服务系统 |
| 文档版本 | v1.1 |
| 编写日期 | 2026-05-21 |
| 修订日期 | 2026-05-23 |

---

## 1 系统概述

面向校园/园区的智能食堂系统，支持用户在线点餐、商家接单备餐、用户扫码/取餐码取餐、食堂大屏实时显示取餐队列。采用 Spring Cloud Alibaba 微服务架构，部署于 K3S 集群。

---

## 2 技术选型

| 分类 | 组件 | 版本 | 备注 |
| --- | --- | --- | --- |
| 语言框架 | Java 17 + Spring Boot | 3.2.x | LTS |
| 微服务套件 | Spring Cloud + SCA | 2023.0.x | 与 Boot 3.2 对齐 |
| 注册/配置 | Nacos | 2.3.x | standalone |
| 网关 | Spring Cloud Gateway (reactive) | 4.x | 原生 WS 代理 |
| 限流熔断 | Sentinel | 1.8.x | 规则持久化到 Nacos |
| 服务调用 | OpenFeign | - | 同步主链路 |
| 消息 | RocketMQ | 5.x | 延时消息 + 广播 |
| 持久化 | MySQL | 8.0 | 一服务一 schema |
| 缓存 | Redis | 7.x | 库存 + 队列 + JWT 黑名单 |
| 认证 | JWT (HS256) | - | 双 Token + Redis 黑名单 |
| 部署 | K3S | 1.28+ | 单 master + 2 worker |

---

## 3 软件架构图

### 3.1 系统总体架构图

```mermaid
graph TB
    subgraph Client["客户端层"]
        U["用户端<br/>(H5/App)"]
        M["商家端<br/>(H5/App)"]
        S["食堂大屏<br/>(WebSocket)"]
        A["管理后台<br/>(H5)"]
    end

    subgraph Gateway["API 网关层"]
        GW["Gateway Service<br/>:8080 | NodePort :30080"]
        R1["路由转发<br/>(Nacos)"]
        R2["JWT校验<br/>+黑名单"]
        R3["限流控制<br/>(Sentinel+Redis)"]
        R4["WS 代理<br/>(lb:ws://)"]
        GW --- R1 & R2 & R3 & R4
    end

    subgraph Services["微服务层"]
        US["用户服务 :8081<br/>注册登录 | JWT颁发<br/>资料管理 | Token刷新"]
        MS["菜品服务 :8082<br/>菜品CRUD | 每日菜单<br/>库存管理 | 上下架"]
        OS["订单服务 :8083<br/>下单 | 状态机<br/>延时取消 | 取餐码"]
        PS["取餐服务 :8084<br/>FIFO队列 | 叫号<br/>核销 | WS推送"]
    end

    subgraph Infra["基础设施层"]
        NC["Nacos<br/>注册+配置 :8848"]
        DB["MySQL<br/>3 schema :3306"]
        RD["Redis<br/>库存+队列 :6379"]
        MQ["RocketMQ<br/>延时消息 :9876"]
    end

    Client -->|"HTTP/WS"| Gateway
    Gateway -->|"路由转发"| Services
    MS <-->|"Feign /internal/stock/"| OS
    OS -->|"Feign /internal/queues/"| PS
    PS -->|"Feign /internal/orders/"| OS
    Services --> Infra
    OS -.->|"延时消息"| MQ
```

### 3.2 服务间调用关系图

```mermaid
graph TD
    GW["Gateway :8080"] --> US["User Service :8081"]
    GW --> MS["Menu Service :8082"]
    GW --> OS["Order Service :8083"]
    GW --> PS["Pickup Service :8084"]

    OS -->|"Feign: 查菜品/商户"| MS
    OS -->|"Feign: 扣减/回滚库存"| MS
    OS -->|"Feign: 入队"| PS
    PS -->|"Feign: 标记已取餐"| OS

    OS -.->|"发送延时消息"| MQ["RocketMQ"]
    MQ -.->|"30min 超时回调"| OS

    PS -->|"WebSocket 广播"| Screen["食堂大屏"]
```

### 3.3 订单状态机图

```mermaid
stateDiagram-v2
    [*] --> PLACED: 用户下单
    PLACED --> ACCEPTED: 商家接单
    ACCEPTED --> PREPARING: 开始制作
    PREPARING --> WAITING_PICKUP: 制作完成/生成取餐码
    WAITING_PICKUP --> PICKED_UP: 用户取餐核销
    PLACED --> CANCELED: 取消(超时/用户主动)
    ACCEPTED --> CANCELED: 取消(超时/用户主动)
    CANCELED --> [*]
    PICKED_UP --> [*]
```

---

## 4 微服务划分

| 服务 | 端口 | 职责 | 数据库 | Redis 用途 |
| --- | --- | --- | --- | --- |
| gateway-service | 8080 (NodePort 30080) | 路由、JWT、限流、WS 代理 | - | 限流计数 + JWT 黑名单 |
| user-service | 8081 | 注册登录、JWT、资料管理 | canteen_user | Token 黑名单 |
| menu-service | 8082 | 菜品CRUD、菜单发布、库存管理 | canteen_menu | 库存缓存(Lua原子扣减) |
| order-service | 8083 | 下单、状态机、延时取消、取餐码 | canteen_order | 幂等键 + 取餐码序列 |
| pickup-service | 8084 | FIFO队列、叫号、核销、WS推送 | 仅 Redis | 队列 + 反向索引 |

---

## 5 关键流程

### 5.1 下单流程

```mermaid
sequenceDiagram
    participant C as 客户端
    participant G as Gateway
    participant O as Order Service
    participant M as Menu Service
    participant R as Redis
    participant MQ as RocketMQ

    C->>G: POST /api/order/orders
    G->>O: 转发(注入 X-User-Id)
    O->>R: SETNX order:idem:{key} EX 60 (幂等校验)
    O->>M: Feign: 查询菜品信息(价格/上下架)
    M-->>O: 返回菜品信息
    O->>M: Feign: POST /internal/stock/deduct (Lua原子扣减)
    M->>R: Lua脚本扣减 stock:{dishId}
    M-->>O: 扣减结果
    O->>O: 本地事务: 写订单+订单明细
    O->>MQ: 发送延时消息 order.timeout (delay=30min)
    O-->>C: 返回 {orderId, totalCents, status=PLACED}
```

### 5.2 取餐流程

```mermaid
sequenceDiagram
    participant MC as 商家端
    participant O as Order Service
    participant P as Pickup Service
    participant R as Redis
    participant WS as WebSocket
    participant U as 用户端

    MC->>O: "制作完成" → readyForPickup
    O->>O: 状态机: PREPARING → WAITING_PICKUP
    O->>R: INCR 生成6位取餐码
    O->>P: Feign: /internal/queues/enqueue
    P->>R: RPUSH queue:{counterId}

    MC->>P: "叫号" → call
    P->>R: LPOP queue:{counterId}
    P->>R: SET queue:current:{counterId}
    P->>WS: 广播 /topic/screen/{counterId}
    WS-->>U: 大屏实时更新

    U->>P: "核销" → verify(pickupCode)
    P->>R: GET code:{counterId}:{pickupCode} → orderId
    P->>O: Feign: /internal/orders/{id}/picked
    O->>O: 状态机: WAITING_PICKUP → PICKED_UP
    P->>R: DEL 清理队列/索引数据
    P-->>U: 核销成功
```

---

## 6 数据库设计

每个服务独立 schema，通过 ID 引用，不建外键。所有表默认带 `id BIGINT PK`、`created_at`、`updated_at`、`deleted_at`。

| Schema | 主要表 | 说明 |
| --- | --- | --- |
| canteen_user | `user`, `refresh_token` | 用户信息 + Token 记录 |
| canteen_menu | `merchant`, `dish`, `daily_menu`, `daily_menu_item` | 商户 + 菜品 + 每日菜单 |
| canteen_order | `orders`, `order_item` | 订单 + 订单明细 |
| (pickup) | 仅 Redis | 队列 + 反向索引 |

### 6.1 ER 图

```mermaid
erDiagram
    USER ||--o{ REFRESH_TOKEN : has
    MERCHANT ||--o{ DISH : owns
    MERCHANT ||--o{ DAILY_MENU : publishes
    DAILY_MENU ||--o{ DAILY_MENU_ITEM : contains
    DISH ||--o{ DAILY_MENU_ITEM : references
    USER ||--o{ ORDERS : places
    MERCHANT ||--o{ ORDERS : receives
    ORDERS ||--o{ ORDER_ITEM : contains

    USER {
        bigint id PK
        varchar phone UK
        varchar student_no UK
        varchar password_hash
        varchar nickname
        varchar avatar
        tinyint status
    }

    DISH {
        bigint id PK
        bigint merchant_id FK
        varchar name
        int price_cents
        tinyint on_shelf
        int threshold
    }

    ORDERS {
        bigint id PK
        bigint user_id
        bigint merchant_id
        varchar counter_id
        varchar status
        int total_cents
        varchar pickup_code
    }

    ORDER_ITEM {
        bigint id PK
        bigint order_id FK
        bigint dish_id
        varchar dish_name_snapshot
        int unit_price
        int quantity
    }
```

---

## 7 非功能需求

| 类别 | 要求 | 实现方式 |
| --- | --- | --- |
| 性能 | 单节点 QPS ≥ 200, p95 ≤ 500ms | Redis Lua 原子操作 + 连接池 |
| 安全 | 密码 BCrypt + JWT 可吊销 + 内部 Token 隔离 | JwtTokenProvider + InternalTokenInterceptor |
| 可用性 | 任一服务宕机不影响其他 | 服务隔离 + 网关统一异常处理 |
| 可扩展 | 无状态可水平扩容 | Redis 外置状态 + K8s HPA |
| 可观测 | 日志统一 + 健康检查 | TraceId + Actuator + Prometheus |

---

## 8 统一错误码

| 分段 | 含义 |
| --- | --- |
| `0` | 成功 |
| `10xxx` | 用户域（10001 账号已存在, 10002 密码错误, 10003 Token无效或已过期, 10004 Token已过期, 10005 用户不存在, 10006 手机号已注册, 10007 验证码错误, 10008 账号已被禁用） |
| `20xxx` | 菜品域（20001 库存不足, 20002 菜品已下架, 20003 菜品不存在, 20004 今日菜单已发布, 20005 商户不存在） |
| `30xxx` | 订单域（30001 订单状态非法, 30002 订单已过期, 30003 订单不存在, 30004 订单当前状态不可取消, 30005 重复请求） |
| `40xxx` | 取餐域（40001 取餐码不存在, 40002 重复核销, 40003 队列为空） |
| `50xxx` | 网关/系统（50001 请求过于频繁, 50002 鉴权失败, 50003 服务暂不可用） |
| `90000` | 未知异常 |
