# 测试用例

| 项目 | 内容 |
| --- | --- |
| 项目名称 | 智能食堂点餐与取餐微服务系统 |
| 文档版本 | v1.0 |
| 编写日期 | 2026-05-21 |

---

## 1 用户服务测试用例

### TC-U-01 用户注册 — 正常注册

| 项目 | 内容 |
| --- | --- |
| 前置条件 | 无 |
| 输入 | phone=13800138001, studentNo=2024001, password=Test1234, nickname=张三 |
| 执行步骤 | POST /api/user/auth/register |
| 预期结果 | HTTP 200, code=0, 返回 accessToken + refreshToken, userId 非空 |
| 后置条件 | 数据库 user 表新增一条记录，password_hash 为 BCrypt 加密 |

### TC-U-02 用户注册 — 手机号重复

| 项目 | 内容 |
| --- | --- |
| 前置条件 | 手机号 13800138001 已注册 |
| 输入 | phone=13800138001, password=Test1234 |
| 执行步骤 | POST /api/user/auth/register |
| 预期结果 | HTTP 200, code=10006, message="手机号已注册" |

### TC-U-03 用户登录 — 密码登录成功

| 项目 | 内容 |
| --- | --- |
| 前置条件 | 用户已注册 |
| 输入 | phone=13800138001, loginType=password, password=Test1234 |
| 执行步骤 | POST /api/user/auth/login |
| 预期结果 | HTTP 200, code=0, 返回 accessToken + refreshToken |

### TC-U-04 用户登录 — 验证码登录成功

| 项目 | 内容 |
| --- | --- |
| 前置条件 | 用户已注册 |
| 输入 | phone=13800138001, loginType=sms, verifyCode=123456 |
| 执行步骤 | POST /api/user/auth/login |
| 预期结果 | HTTP 200, code=0, 返回 accessToken + refreshToken |

### TC-U-05 用户登录 — 密码错误

| 项目 | 内容 |
| --- | --- |
| 前置条件 | 用户已注册 |
| 输入 | phone=13800138001, loginType=password, password=WrongPwd |
| 执行步骤 | POST /api/user/auth/login |
| 预期结果 | HTTP 200, code=10004, message="密码错误" |

### TC-U-06 Token 刷新

| 项目 | 内容 |
| --- | --- |
| 前置条件 | 用户已登录，持有有效 refreshToken |
| 输入 | refreshToken={有效的refreshToken} |
| 执行步骤 | POST /api/user/auth/refresh |
| 预期结果 | HTTP 200, code=0, 返回新的 accessToken + refreshToken，旧的 refreshToken 失效 |

### TC-U-07 用户登出

| 项目 | 内容 |
| --- | --- |
| 前置条件 | 用户已登录 |
| 输入 | Authorization: Bearer {accessToken} |
| 执行步骤 | POST /api/user/auth/logout |
| 预期结果 | HTTP 200, code=0；旧 accessToken 加入 Redis 黑名单，后续请求返回 401 |

### TC-U-08 查询用户资料

| 项目 | 内容 |
| --- | --- |
| 前置条件 | 用户已登录 |
| 输入 | Authorization: Bearer {token} |
| 执行步骤 | GET /api/user/users/me |
| 预期结果 | HTTP 200, 返回 {id, phone, nickname, avatar, status} |

---

## 2 菜品与菜单服务测试用例

### TC-M-01 新增菜品

| 项目 | 内容 |
| --- | --- |
| 前置条件 | 商户已存在（id=1） |
| 输入 | merchantId=1, name=宫保鸡丁, priceCents=1200, threshold=5 |
| 执行步骤 | POST /api/menu/dishes |
| 预期结果 | HTTP 200, code=0, 返回 {id, name="宫保鸡丁", onShelf=1} |

### TC-M-02 修改菜品

| 项目 | 内容 |
| --- | --- |
| 前置条件 | 菜品 id=1 存在 |
| 输入 | name=宫保鸡丁(大份), priceCents=1800 |
| 执行步骤 | PUT /api/menu/dishes/1 |
| 预期结果 | HTTP 200, name 和 priceCents 已更新 |

### TC-M-03 菜品下架

| 项目 | 内容 |
| --- | --- |
| 前置条件 | 菜品 id=1 存在且 onShelf=1 |
| 输入 | 无 |
| 执行步骤 | PUT /api/menu/dishes/1/on-shelf |
| 预期结果 | HTTP 200, onShelf 变为 0 |

### TC-M-04 发布每日菜单

| 项目 | 内容 |
| --- | --- |
| 前置条件 | 商户 id=1 当日未发布菜单 |
| 输入 | merchantId=1, bizDate=今天, sellStart=10:30, sellEnd=13:30, items=[{dishId=1, stockInit=50}] |
| 执行步骤 | POST /api/menu/daily |
| 预期结果 | HTTP 200, code=0, Redis 中 stock:1=50 |

### TC-M-05 重复发布每日菜单

| 项目 | 内容 |
| --- | --- |
| 前置条件 | 商户 id=1 当日已发布菜单 |
| 输入 | merchantId=1, bizDate=今天 |
| 执行步骤 | POST /api/menu/daily |
| 预期结果 | HTTP 200, code=20005, message="今日菜单已发布" |

### TC-M-06 库存扣减成功

| 项目 | 内容 |
| --- | --- |
| 前置条件 | Redis stock:1=50 |
| 输入 | items=[{dishId=1, quantity=2}] |
| 执行步骤 | POST /internal/stock/deduct |
| 预期结果 | HTTP 200, data=true, Redis stock:1=48 |

### TC-M-07 库存扣减失败 — 库存不足

| 项目 | 内容 |
| --- | --- |
| 前置条件 | Redis stock:1=1 |
| 输入 | items=[{dishId=1, quantity=5}] |
| 执行步骤 | POST /internal/stock/deduct |
| 预期结果 | HTTP 200, data=false, Redis stock:1 不变 |

---

## 3 订单服务测试用例

### TC-O-01 下单成功

| 项目 | 内容 |
| --- | --- |
| 前置条件 | 用户已登录，菜品上架且有库存 |
| 输入 | merchantId=1, items=[{dishId=1, quantity=2}], Header: X-User-Id=1, Idempotency-Key=unique-key-1 |
| 执行步骤 | POST /api/order/orders |
| 预期结果 | HTTP 200, code=0, data.status=PLACED, data.totalCents=2400, RocketMQ 发送延时消息 |

### TC-O-02 下单 — 重复幂等键

| 项目 | 内容 |
| --- | --- |
| 前置条件 | 同一个 Idempotency-Key 已使用过 |
| 输入 | 同 TC-O-01，Idempotency-Key=unique-key-1 |
| 执行步骤 | POST /api/order/orders |
| 预期结果 | HTTP 200, code=30005, message="重复请求" |

### TC-O-03 下单 — 菜品已下架

| 项目 | 内容 |
| --- | --- |
| 前置条件 | 菜品 onShelf=0 |
| 输入 | items=[{dishId=2, quantity=1}], dishId=2 已下架 |
| 执行步骤 | POST /api/order/orders |
| 预期结果 | HTTP 200, code=20002, message="菜品已下架" |

### TC-O-04 下单 — 库存不足

| 项目 | 内容 |
| --- | --- |
| 前置条件 | 菜品库存不足 |
| 输入 | items=[{dishId=1, quantity=999}] |
| 执行步骤 | POST /api/order/orders |
| 预期结果 | HTTP 200, code=20003, message="库存不足" |

### TC-O-05 商家接单

| 项目 | 内容 |
| --- | --- |
| 前置条件 | 订单 status=PLACED |
| 输入 | orderId=1 |
| 执行步骤 | PUT /api/order/orders/1/accept |
| 预期结果 | HTTP 200, data.status=ACCEPTED |

### TC-O-06 制作完成 — 生成取餐码并入队

| 项目 | 内容 |
| --- | --- |
| 前置条件 | 订单 status=PREPARING |
| 输入 | orderId=1 |
| 执行步骤 | PUT /api/order/orders/1/ready |
| 预期结果 | HTTP 200, data.status=WAITING_PICKUP, data.pickupCode 非空, pickup-service 队列新增一条 |

### TC-O-07 取消订单 — 正常取消

| 项目 | 内容 |
| --- | --- |
| 前置条件 | 订单 status=PLACED 或 ACCEPTED |
| 输入 | orderId=1, reason=不想要了 |
| 执行步骤 | PUT /api/order/orders/1/cancel?reason=不想要了 |
| 预期结果 | HTTP 200, data.status=CANCELED, 库存已回滚 |

### TC-O-08 取消订单 — 状态不允许

| 项目 | 内容 |
| --- | --- |
| 前置条件 | 订单 status=PREPARING |
| 输入 | orderId=1 |
| 执行步骤 | PUT /api/order/orders/1/cancel |
| 预期结果 | HTTP 200, code=30002, message 包含 "状态非法" |

### TC-O-09 超时自动取消

| 项目 | 内容 |
| --- | --- |
| 前置条件 | 订单下单 30min 后仍为 PLACED |
| 输入 | RocketMQ 延时消息消费 |
| 执行步骤 | OrderTimeoutListener 自动触发 |
| 预期结果 | 订单 status=CANCELED, cancelReason="超时自动取消", 库存已回滚 |

---

## 4 取餐排队服务测试用例

### TC-P-01 入队

| 项目 | 内容 |
| --- | --- |
| 前置条件 | 无 |
| 输入 | orderId=1, pickupCode=C01000001, userId=1, counterId=C01 |
| 执行步骤 | POST /internal/queues/enqueue |
| 预期结果 | HTTP 200, code=0, Redis queue:C01 新增一个元素, code:C01:C01000001=1 |

### TC-P-02 叫号

| 项目 | 内容 |
| --- | --- |
| 前置条件 | queue:C01 中至少一条记录 |
| 输入 | counterId=C01 |
| 执行步骤 | POST /api/pickup/queues/C01/call |
| 预期结果 | HTTP 200, 返回队首 QueueEntry, WS 推送 CallEvent, queue:current:C01 已设置 |

### TC-P-03 叫号 — 队列为空

| 项目 | 内容 |
| --- | --- |
| 前置条件 | queue:C01 为空 |
| 输入 | counterId=C01 |
| 执行步骤 | POST /api/pickup/queues/C01/call |
| 预期结果 | HTTP 200, code=40001, message="取餐队列为空" |

### TC-P-04 核销

| 项目 | 内容 |
| --- | --- |
| 前置条件 | 取餐码 C01000001 存在于反向索引 |
| 输入 | counterId=C01, pickupCode=C01000001 |
| 执行步骤 | POST /api/pickup/pickups/verify |
| 预期结果 | HTTP 200, code=0, order-service 标记订单 PICKED_UP, 反向索引已删除 |

### TC-P-05 核销 — 取餐码不存在

| 项目 | 内容 |
| --- | --- |
| 前置条件 | 取餐码不存在 |
| 输入 | pickupCode=INVALID |
| 执行步骤 | POST /api/pickup/pickups/verify |
| 预期结果 | HTTP 200, code=40002, message="取餐码不存在" |

### TC-P-06 大屏数据查询

| 项目 | 内容 |
| --- | --- |
| 前置条件 | 队列中有数据 |
| 输入 | counterId=C01 |
| 执行步骤 | GET /api/pickup/queues/C01 |
| 预期结果 | HTTP 200, data 包含 currentCalling, waitingList, historyList |

### TC-P-07 WebSocket 连接与推送

| 项目 | 内容 |
| --- | --- |
| 前置条件 | WebSocket 端点可用 |
| 输入 | 连接 ws://host:30080/ws/screen, 订阅 /topic/screen/C01 |
| 执行步骤 | 商家触发叫号 |
| 预期结果 | 客户端收到 CallEvent JSON 消息 |

---

## 5 网关服务测试用例

### TC-G-01 路由转发 — 正常请求

| 项目 | 内容 |
| --- | --- |
| 前置条件 | 服务已注册到 Nacos |
| 输入 | GET /api/user/users/me, Authorization: Bearer {validToken} |
| 执行步骤 | 请求经网关转发 |
| 预期结果 | 网关正确路由到 user-service, 响应 200 |

### TC-G-02 JWT 校验 — 无 Token

| 项目 | 内容 |
| --- | --- |
| 前置条件 | 无 |
| 输入 | GET /api/order/orders（无 Authorization header） |
| 执行步骤 | 请求经网关 |
| 预期结果 | HTTP 401 Unauthorized |

### TC-G-03 JWT 校验 — 黑名单 Token

| 项目 | 内容 |
| --- | --- |
| 前置条件 | Token 已被登出加入 Redis 黑名单 |
| 输入 | Authorization: Bearer {blacklistedToken} |
| 执行步骤 | 请求经网关 |
| 预期结果 | HTTP 401 Unauthorized |

### TC-G-04 白名单放行

| 项目 | 内容 |
| --- | --- |
| 前置条件 | 无 |
| 输入 | POST /api/user/auth/register（无 Token） |
| 执行步骤 | 请求经网关 |
| 预期结果 | 网关放行，正常转发到 user-service |

### TC-G-05 IP 级限流

| 项目 | 内容 |
| --- | --- |
| 前置条件 | 同一 IP 短时间大量请求 |
| 输入 | 连续发送 >100 次/min 请求 |
| 执行步骤 | 请求经网关 |
| 预期结果 | 超出阈值后返回 HTTP 429 Too Many Requests |

### TC-G-06 WebSocket 代理

| 项目 | 内容 |
| --- | --- |
| 前置条件 | pickup-service 运行正常 |
| 输入 | ws://host:30080/ws/screen/counter/C01 |
| 执行步骤 | 建立 WS 连接 |
| 预期结果 | 连接成功建立，可接收推送消息 |

---

## 6 异常场景测试用例

### TC-E-01 服务不可用

| 项目 | 内容 |
| --- | --- |
| 前置条件 | menu-service 停止运行 |
| 输入 | GET /api/menu/dishes |
| 执行步骤 | 请求经网关 |
| 预期结果 | 网关返回 503 Service Unavailable, 其他服务不受影响 |

### TC-E-02 数据库连接失败

| 项目 | 内容 |
| --- | --- |
| 前置条件 | MySQL 不可达 |
| 输入 | POST /api/user/auth/login |
| 执行步骤 | 请求到 user-service |
| 预期结果 | 返回 HTTP 500, 错误信息不暴露数据库细节 |

### TC-E-03 Redis 不可达

| 项目 | 内容 |
| --- | --- |
| 前置条件 | Redis 不可达 |
| 输入 | POST /api/order/orders（需要 Redis 幂等校验） |
| 执行步骤 | 下单请求 |
| 预期结果 | 返回 HTTP 500 或降级响应, 不产生脏数据 |
